import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Card extends Component {
  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      null
    );
  }
}

export default Card;
